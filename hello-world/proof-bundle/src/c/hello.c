/* Smallest C program that satisfies contract_declaration.toml C01:
 * writes the exact line "Hello, world!\n" to stdout and exits zero. */
#include <stdio.h>

int main(void) {
    puts("Hello, world!");
    return 0;
}
