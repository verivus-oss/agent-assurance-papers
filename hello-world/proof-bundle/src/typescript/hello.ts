// Smallest TypeScript program that satisfies contract_declaration.toml C01:
// writes the exact line "Hello, world!\n" to stdout and exits zero.
// Executed via `node --experimental-strip-types` or compiled via `tsc`.
function main(): void {
  console.log("Hello, world!");
}

main();
