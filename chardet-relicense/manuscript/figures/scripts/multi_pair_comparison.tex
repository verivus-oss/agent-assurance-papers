% Generated by chardet-relicense/proof-bundle/build_multi_pair.py
% then refreshed by Phase-1a integration (v2-phase1a-merged) after merging
% v2-phase1a-{bp,d,e,c,q} so all signals are populated for all three pairs.
% Phase-1b (v2-phase1b-n) augmented C06a / C06a' / C06c / C06f rows with
% 95% percentile-bootstrap confidence intervals (1000 resamples,
% seed=20260528). Per-pair displayed cells are "point [lo, hi]" where
% the point is the unchanged Phase-1a number and the bracket is the CI.
% Resampling unit: implementation files (independent each side) for
% C06a/C06a'/C06c; matched function pairs for C06f. See
% manuscript/v2-phase1b-bootstrap-methodology.md for full methodology.
% DO NOT EDIT BY HAND; re-run detect.sh per pair + this script.
%
% Resolved commit SHAs:
%   chardet 5.0.0          = 21bc6be (tag object) -> fbb2ec6 (commit, 5.0.0^{})
%   chardet 6.0.0          = 8a4636b
%   chardet 7.0.0          = 4b89d62
%   charset-normalizer 3.4.7 = 0f07891
\begin{table}[t]
\centering
\caption{Multi-pair calibration of the chardet-relicensing harness with the v2 revision's expanded signal set, with 95\% percentile-bootstrap confidence intervals on the four structural-similarity signals (Phase-1b). Each column is the same eight signals run against a different pair: v6 vs v7 (the original LGPL/MIT pair, leftmost), v5 vs v6 (conventional same-project release-to-release calibration), v6 vs charset\_normalizer (independent same-domain detector calibration). Cells of the form ``$0.881\,[0.74,0.95]$'' report the point estimate followed by the [2.5\%, 97.5\%] bootstrap quantiles over 1000 resamples (seed=20260528). For C06a / C06a$'$ / C06c the resampling unit is implementation files (independent each side, with replacement); for C06f it is matched function pairs. Commit SHAs resolved at integration time: chardet 5.0.0=21bc6be (tag object; peeled commit fbb2ec6), 6.0.0=8a4636b, 7.0.0=4b89d62; charset-normalizer 3.4.7=0f07891.}
\label{tab:multi-pair-calibration}
\small
\begin{tabular}{lrrr}
\toprule
Signal & v6\,vs\,v7 & v5\,vs\,v6 & v6\,vs\,c\_n \\
\midrule
AUX1: literal whitespace-norm SHA-256 matches & 0 & 0 & 0 \\
C06a: call-graph topology similarity (0--1) & $0.881\,[0.74,0.95]$ & $0.930\,[0.79,0.97]$ & $0.922\,[0.75,0.95]$ \\
C06a$'$: Weisfeiler-Lehman ($k{=}4$) cosine (0--1) & $0.587\,[0.43,0.69]$ & $0.902\,[0.75,0.89]^{\dagger}$ & $0.872\,[0.48,0.86]^{\dagger}$ \\
C06b: 3rd-party import-edge Jaccard (0--1), R1--R5 audit & 0.667 & 0.250 & 0.000 \\
C06c: control-flow histogram cosine (0--1) & $0.984\,[0.94,1.00]$ & $0.995\,[0.96,1.00]$ & $0.999\,[0.95,1.00]$ \\
C06d: \texttt{\_\_all\_\_} shared symbols & 5 & 5 & 3 \\
\quad of which strict-signature equal & 3 & 3 & 2 \\
\quad of which diverged & 2 & 2 & 1 \\
C06d (per-method, public classes only): strict / renamed / diverged / added\_in\_v7 / removed\_class
 & 1 / 0 / 1 / 1 / 0 & 0 / 0 / 1 / 1 / 0 & 0 / 0 / 0 / 2 / 2 \\
C06e: behavioural exact-match rate (random-fuzz 1000) & 0.000 & 0.968 & 0.000 \\
C06e: behavioural bucket-match rate (random-fuzz 1000) & 0.000 & 0.968 & 0.000 \\
C06e: behavioural normalized-match rate (realistic 64) & 0.625 & 0.688 & 0.594 \\
C06f: per-function shape similarity over matched pairs & $0.913\,[0.89,0.94]$ & $0.982\,[0.97,0.99]$ & $0.796\,[0.75,0.84]$ \\
\quad matched-pair count / v6 functions & 31 / 177 & 103 / 161 & 77 / 177 \\
\bottomrule
\end{tabular}

\smallskip
\footnotesize
$^{\dagger}$~Point estimate falls just outside the bootstrap CI (above the upper bound). This is a known artefact of file-level resampling with replacement: the bootstrap's average sample contains $\approx 0.63 N$ unique files, and the WL kernel's multiset cosine is sensitive to whole-file dropout because file dropout removes entire neighbourhood subtrees from the call graph. The effect is most pronounced on pairs where the WL multiset is dominated by a few large files (v5/v6 and v6/charset\_normalizer). The C06a $1-$mean(reldiff) similarity, in contrast, is robust to file dropout because its 8 features are averages over the surviving subgraph; all three C06a point estimates sit inside their CIs. We retain the bootstrap CI as a conservative bound rather than recompute under a different resampling unit, and discuss the asymmetry in the methodology paragraph.
\end{table}
